package: 'systemd'

config:
  - branch: 'main'
    release: 'c9s'
  - branch: 'rhel-9.0.0'
    release: '9.0.0'
  - branch: 'rhel-9.1.0'
    release: '9.1.0'

rules:
  bugzillaReference:
    label: 'needs-bz'

  jiraReference: 'SomeRandomValue'

  review:
    label: 'needs-review'

  ci:
    label: 'needs-ci'

  upstreamReference:
    label: 'needs-upstream'

  flags:
    label: 'needs-flags'
    flags: ['qa_ack', 'devel_ack', 'release']
  