// import { Config } from '../../src/models/config/config.model';

// import { PlumberConfig } from '../../src/models/config/rules/plumber.config';

describe('Config Object', () => {
  //   let config: Config[];

  //   beforeEach(() => {
  //     const data: PlumberConfig[] = [
  //       {
  //         package: 'test',

  //         config: [{ branch: 'main', release: 'c9s' }],

  //         rules: {
  //           bugzillaReference: {
  //             label: 'needs-bz',
  //           },
  //           flags: {
  //             blocking: true,
  //             label: 'needs-flags',
  //             flags: ['qa_ack', 'devel_ack', 'release'],
  //           },
  //         },
  //       },
  //     ];

  //     config = data.map(item => new Config(item));
  //   });

  //   it('can be instantiated', () => {
  //     config.map(item => expect(item).toBeDefined());
  //   });

  it.todo('can be validated');

  it.todo('can provide data');
});
